# P0 统一皮肤安全清理

本轮按照 `Documentation/代码审查/2026-09-17-统一皮肤与可维护性审查.md` 的 P0 计划执行，目标是 **视觉零回归**，先清理历史覆盖和旧架构，不在这一轮重新设计皮肤。

## 完成内容

### 1. 强制入口文档更新

已更新：

- `Documentation/工作交接.md`
- `Documentation/项目概览.md`
- `Documentation/UI空间与状态架构.md`

当前文档统一记录：

- Top Metric Shortcut = `人口 / 金钱 / 贸易值 / 军事值`；
- 顶部只显示 `Icon + Number`；
- 顶部四项只做 Quick Entry，不拥有 Selected / Active；
- 下方 Control Tray 才是 Management 主导航；
- 一级管理域为 `城市 / 经济 / 库存 / 政策 / 军事`；
- Inventory 包含 `总览 / 城市仓库 / 周边村庄`；
- Gameplay 正式配色为冷黛黑 / Graphite + Paper White + 熟铜。

### 2. Review Study 与 Runtime 分离

新增正式生产基线：

- `src/ui/wanhu-mist-glass-production.css`

历史实验样式移动到：

- `src/review/styles/glass-study.css`
- `src/review/styles/edge-elevation-study.css`

`src/main.tsx` 不再把 A/B/C Study 常驻加载进正式 cascade。仅当 Review URL 明确带：

- `?study=glass`
- `?study=edge`

时动态加载对应 Study CSS。

相关 Playwright Study 脚本同步改为显式 opt-in。

### 3. Character 历史方案清理

`src/ui/wanhu-character.css` 删除已经废弃的：

- Top HUD 梁头；
- Workspace Header 梁头；
- 旧标题装饰节点；
- 对应废弃 animation。

`src/ui/wanhu-icon-led-header.css` 同时删除过去用于强制关闭这些梁头的 `content:none` 防回退覆盖。

当前 Character 只保留仍有语义的状态语言：

- Main Dock 台基式 Selected；
- Filter 小节点；
- Mode Rail 状态线；
- Workspace settle motion。

### 4. Legacy Gameplay CSS 删除

`src/gameplay/city-management.css` 删除已退出运行结构的：

- Global HUD；
- Quick Controls；
- City Management Rail；
- 旧 Map View Panel；
- 已不再渲染的 Management 一级 Tabs；
- 对旧 Global HUD 的 Management 特殊覆盖。

`src/styles.css` 删除：

- `.hud-group`；
- `.city-status`；
- `.resource-strip`；
- `.quick-controls`；
- Right Edge Flyout 与 Reset Button 旧样式。

仍在使用的 `.command-bar`、`.workspace`、`.tool-overlay` 基础几何没有因为名称较旧而误删。

## 复核

临时分支：`tmp-p0-skin-cleanup-v1`

Build 与完整 Visual Review 均通过，包括：

- Gameplay；
- Top Shell；
- Workspace / Inspector；
- Environment；
- Tool；
- Management / Inventory；
- Day / Night；
- Glass Study；
- Edge Study；
- Top Metric Shortcut 交互。

人工检查的核心 Gameplay、Workspace、Environment、Night、Inventory 画面均未发现视觉回归。

对比清理前 Review Artifact 后，`02-gameplay`、`03-design-bridge`、`09-weather-context`、`35-weather-night`、`36-gameplay-night`、`39-workspace-night`、`54-top-metric-shortcuts` 等核心运行截图解码像素完全一致。少数包含入场动画 / 弹层时序的 Review 截图存在轻微截图时序差异，不属于皮肤变化。

## 后续

P0 结束后再进入 P1：

`Theme Tokens → Surface System → Control System → Component Geometry`

P1 的目标是把目前仍存在的 Tonal / HUD Glass / Contrast / Workspace Integration 等多层正式覆盖逐步折回唯一 Owner，而不是继续增加新的 `Pass vN` 文件。
