# 工作交接

这是新对话、新 AI 或新开发者接手《万户天工》UI Prototype 时的首要入口。

## 当前目标

继续完成《万户天工》的 UI 美术与交互原型，使其成为后续 Unity UI Toolkit 正式实现的可靠设计参考。

当前阶段重点不是增加更多功能，而是把已经形成的视觉语言推广到剩余页面：**信息保持精简，视觉通过 Tone、真实内容图像、材质、排版节奏与轻动效增加层级。**

## 当前整体状态

已经完成：

- Main Menu；
- New Game 第一阶段；
- Gameplay HUD；
- Quick Controls；
- Main Dock；
- Utility Toolbar；
- Building Workspace；
- BuildingPlacementOverlay；
- BuildingPlacementDock；
- GameplayOperationHints；
- Camera / Weather Right Edge Flyout；
- Pause Layer；
- Pause Save；
- Pause Settings；
- 全屏 Archive / Load；
- 全屏 Settings；
- 固定 Review Scenario；
- GitHub Actions + Playwright 自动截图；
- Visual Cleanup Pass 1 / 2；
- Visual Language Pass 3 基准页面。

Visual Review 当前 16 个主要状态，最近一轮完整截图成功。

## 已确认的视觉基准

### Settings

- 一个全屏 Settings Space；
- 一个主 Surface，内部按真实功能组组织；
- 可以双栏，但两栏都必须承担真实设置功能；
- 不增加无意义 Preview / Summary；
- 当前 Tab 使用弱暖金 Tone 与细线；
- 控件本身轻量，不恢复重边框。

### Building Workspace

- 建筑目录以图像为主、文字为辅；
- 当前 Prototype 已使用现有真实游戏截图的不同裁切作为过渡缩略图；
- 最终需要真实建筑 RenderTexture / 建筑预览截图替换这些裁切；
- 不允许使用 AI 生成建筑图冒充实际游戏资产；
- Hover / Selected 通过 Tone、轻提亮、轻位移表达。

### BuildingPlacement

- 保持一个主 ToolOverlay；
- Terrain Summary 是明确但轻量的状态带；
- 当前 Position / Massing / Roof 参数区通过局部 Tone 区分；
- 模式切换允许短 Fade / Slide；
- Dock Active / Toggle / Complete 状态可以比普通状态明显；
- 不恢复说明段、重复标题或 Card 堆叠。

## 不要回退

不要重新加入：

- 说明性小字；
- 01/02/03/04 装饰编号；
- 右侧 `>` 箭头；
- 为气氛存在的英文眉题；
- 无意义 Summary / Preview；
- 每个参数 / 内容项都套 Card；
- 粗金边、卷轴、木纹、云纹等仿古装饰。

### 当前视觉原则

- 信息做减法，层级做加法；
- 世界画面优先；
- 黛墨 / 深青灰 Surface；
- 浅纸主文字；
- 暖金仅用于 Selected / Focus / 高价值状态；
- 低圆角、弱边界；
- 优先通过 Tone、留白、材质、真实内容图像和动效丰富画面；
- 如果界面显得单调，先增加内容图像、明暗层级和状态反馈，不要先加文字和边框。

详见 `Documentation/UI设计原则.md`。

## 下一步任务

三个代表页面已经完成基准验证，下一步按以下顺序推广视觉语言。

### 1. Archive

目标：让“游戏组 → 存档时间线 → 存档详情”更像城市档案，而不是三栏文件管理器。

建议：

- 强化 Game Group 的真实城市截图；
- 降低栏与栏之间的硬边界；
- 时间线节点使用更清楚的 Selected / Current Tone；
- Preview 只保留真正需要的存档信息；
- 不增加种子、版本等低频常驻信息。

### 2. Camera / Weather

目标：降低均匀参数表感，增加状态与参数组的节奏。

建议：

- 保持 Right Edge Flyout；
- 使用局部 Tone 区分模式 / 参数组；
- 相机模式和天气状态成为视觉焦点；
- Slider / Segmented / Toggle 继承 Settings 与 BuildingPlacement 的控件语言；
- 不增加说明段。

### 3. New Game

目标：从当前原型卡片升级为正式世界预设选择界面。

建议：

- 世界预设图片优先；
- 河谷平原 / 江南水网 / 北地丘陵需要真实游戏世界截图槽；
- 名称与一行真正影响选择的描述保留；
- 步骤 `世界 / 地形 / 规则 / 开始` 保持简洁；
- 后续从 `App.tsx` 拆成专用 New Game 组件。

### 4. Shared Tooltip

实现正式共享 Tooltip，替换：

- 浏览器原生 `title`；
- BuildingPlacement Dock 当前 CSS 自制 Tooltip；
- 其他组件各自的提示实现。

规则：约 400 ms 延迟、深墨 Surface、弱边界、纯文本为主、不可成为完成基础操作的必要步骤。

### 5. Visual Token / CSS Cleanup

等 Archive / Camera / Weather / New Game / Tooltip 的视觉语言稳定后，再建立系统级 Token，并最后受控整理历史 CSS。

不要在视觉语言尚未完全稳定时提前大规模重构 CSS。

## 关键代码入口

### 顶层

- `src/App.tsx`
- `src/app/ui-state.ts`
- `src/app/scenarios.ts`
- `src/main.tsx`

### Gameplay

- `src/gameplay/GameplayScreen.tsx`
- `src/gameplay/GameplayHUD.tsx`
- `src/gameplay/QuickControls.tsx`
- `src/gameplay/CommandBar.tsx`
- `src/gameplay/GameplayOperationHints.tsx`
- `src/gameplay/RightEdgeFlyout.tsx`
- `src/gameplay/PauseLayer.tsx`

### Workspace / Tool

- `src/workspace/BuildingWorkspace.tsx`
- `src/workspace.css`
- `src/tools/building-placement/BuildingPlacementOverlay.tsx`
- `src/tools/building-placement/BuildingPlacementDock.tsx`
- `src/tools/building-placement/building-placement.css`

### Global Space

- `src/archive/ArchivePanel.tsx`
- `src/archive/archive-panel.css`
- `src/settings/SettingsPanel.tsx`
- `src/settings/settings-panel.css`

### Review

- `.github/workflows/visual-review.yml`
- `src/app/scenarios.ts`
- `scripts/` 下截图脚本

## 已知代码债务

- `src/main.tsx` 仍同时导入旧的全局 CSS 和较新的模块化 CSS；
- `styles.css`、`gameplay-refine.css`、`tool-overlay.css` 等历史样式文件仍存在，后续可在视觉稳定后做受控整合；
- `src/App.tsx` 仍内联 New Game 的 Flow / Preset Card，后续在 New Game 重做时拆出；
- 部分 Prototype 数据仍为硬编码，这是当前阶段允许的；
- Tooltip 尚未形成正式共享组件；
- 建筑缩略图目前使用现有游戏截图裁切作为过渡，仍需要真实建筑预览替换；
- 世界预设图、不同 Game Group 的真实城市截图仍需要真实游戏内容资产补齐。

## 部署说明

最近一次 Visual Review GitHub Actions 成功，16 个状态均生成截图。

当前最近一次 Vercel 状态检查失败原因为 `build-rate-limit`，不是代码构建错误。Vercel 自动部署需要等待平台速率限制恢复后再更新。

## 背景资产

当前真实游戏截图：

- `/public/assets/wanhu-main-menu.png`
- `/public/assets/wanhu-gameplay-city.png`
- `/public/assets/wanhu-gameplay-lake.png`

不要重新生成这些背景图。

## 验证工作流

每轮重要 UI 修改：

1. 修改 GitHub `main`；
2. 确认代码无明显构建风险；
3. 等待 GitHub Actions Visual Review；
4. 下载 `visual-review` Artifact；
5. 人工检查相关状态截图；
6. 若局部修复影响其他 Space，必须继续迭代，不只看目标页面。

Review Scenario 可通过 `?review=...` 直接进入，不应依赖完整点击流程。

## 原始 Unity UI Toolkit 参考

早期用户提供过 `com.jiongxiaxia.wanhu-ui.zip`，其中 Building Placement、GameplayOperationHints、Input Binding 等实现是功能语义参考。

原则：

- 功能与交互语义应参考原 Unity 包；
- 不要盲目复制其旧视觉布局；
- 最终 Unity 实现的 Input Hint 应读取真实 Input System 绑定；
- Web Prototype 的代表性快捷键只用于验证界面。

## 新对话接手建议

先读：

1. `README.md`
2. 本文件
3. `Documentation/UI设计原则.md`
4. `Documentation/UI空间与状态架构.md`
5. 与当前任务相关的组件代码

不要重新从零讨论已经记录在 `Documentation/决策记录/` 中的决策，除非新需求明确推翻它。
