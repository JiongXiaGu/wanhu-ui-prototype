# 工作交接

这是新对话、新 AI 或新开发者接手《万户天工》UI Prototype 时的首要入口。

## 项目定位

本仓库是《万户天工》的 **UI 美术、构图、信息架构与交互验证原型**。

最终运行时 UI 仍然由 **Unity UI Toolkit** 实现：

- UXML：稳定结构；
- USS：布局、视觉状态、Transition；
- C#：状态、事件、数据绑定、输入；
- Sprite / Texture / RenderTexture：视觉资产；
- Painter2D / Mesh：必要自定义绘制；
- URP：场景级视觉能力。

网页不是最终 Web 产品，不要为了浏览器实现方便而设计 Unity 难以维护的核心结构。

正式落地约束：`Documentation/UI Toolkit落地规范.md`

迁移审查：`Documentation/代码审查/2026-09-15-UI Toolkit迁移可行性审查.md`

## 项目入口

- GitHub：`JiongXiaGu/wanhu-ui-prototype`
- Vercel：`https://wanhu-ui-prototype.vercel.app/`
- 技术栈：Vite + React + TypeScript + Playwright + GitHub Actions + Vercel
- 设计基准：1920×1080
- 当前计划使用 URP 全屏 Blur Pass 处理 Pause / Settings / Archive / Management 等场景级背景弱化。

## 当前视觉原则

- 信息做减法，层级做加法；
- 世界画面优先；
- 黛墨 / 深青灰 Surface；
- 浅纸主文字；
- 暖金只用于 Selected / Focus / On / Primary；
- 低圆角、弱边界；
- 解释性小字默认进入 Tooltip；
- 如果界面单调，优先补真实内容、Tone、状态反馈与动效，不堆装饰。

## 当前完成范围

- Main Menu；
- New Game 第一阶段；
- Gameplay Top Shell；
- Gameplay Information Views；
- Gameplay Management Space；
- World Utility Toolbar / Main Dock；
- Building Workspace；
- BuildingPlacementOverlay / Dock；
- GameplayOperationHints；
- Camera / Weather Right Edge Flyout；
- Pause Layer；
- Pause Save / Settings；
- Archive / Load；
- 全屏 Settings；
- Settings 双键位绑定；
- Settings Slider / Select / Toggle 完整交互；
- Settings Disabled / Restore / 键盘交互；
- Settings 安全显示确认与自动回滚；
- GitHub Actions + Playwright Visual Review。

## Gameplay 当前空间基准

正式规则：`Documentation/UI空间与状态架构.md`

Gameplay 内部空间优先级：

`Pause > Tool > Workspace > Management > Gameplay`

### Gameplay Top Shell

顶部已经统一成一个控制系统，不再使用“左侧管理 Rail + 中间 HUD + 右上 Quick Controls”三块分散布局。

Top Shell 第一层是 Persistent Status Row：

- 钱粮；
- 人口；
- 木材；
- 石料；
- 天气；
- 季节 / 时间；
- 相机；
- 时间速度；
- 菜单。

第二层是 Management Navigation Row，语义对应：

`概况 / 户籍 / 财政 / 政策 / 商贸 / 治理 / 军务 / 图层`

正式视觉为**纯图标导航**：常驻不显示文字，只通过 Hover Tooltip 和 ARIA Label 提供名称。第二层整体比第一层更窄，居中挂接在状态栏下方；Selected 使用弱暖金 Tone + 底部细金线。

该第二层只在 Normal Gameplay 与 Management Space 中显示；Workspace / Tool 只保留第一层。

### World Utility Toolbar

右下角现在是稳定的 **World Utility Toolbar / 世界工具栏**，不是某个 Tool 的临时快速条。

当前包含：

- 地图解锁；
- 区域编辑；
- 地形编辑；
- 配色工具；
- 网格吸附；
- 网格显示；
- 范围复制；
- 范围移动；
- 撤销；
- 重做。

规则：

- Normal Gameplay / Workspace / Tool 中保留；
- Management / Pause 中隐藏；
- 只放跨分类、跨工具仍可能使用的世界级辅助能力；
- 不放完成、取消、确认放置等当前任务流程动作；
- 不放道路 / 建筑 / 园林等主建造分类；
- 网格吸附、网格显示使用 `GameplayUiState` 的同一份全局状态，进入 Building Placement 不重置；
- Operation Hints 仍是独立模块，位于工具栏上方，只显示输入说明，不与工具栏合并。

### Normal Gameplay

显示：

- Gameplay Top Shell 两层；
- World Utility Toolbar；
- Main Dock；
- 轻量 OperationHints。

### Management Space

财政、户籍、政策、商贸、治理、军务等复杂系统进入**中央大型阻挡式 Management Space**。

一级管理分类已经移到 Gameplay Top Shell 第二层，因此 Management Surface 内部**不再重复一级 Tab**。

规则：

- 世界停止直接交互；
- 背景城市仍可见，但压暗并轻微模糊；
- Top Shell 两层保留；
- 通过顶部管理图标直接切换当前 Management View；
- 隐藏 World Utility Toolbar、Main Dock、OperationHints、Right Edge Flyout；
- Esc / 关闭按钮回 Normal Gameplay；
- Finance 当前作为复杂页面样例，包含指标、收支趋势、收入构成、税赋设置和财政判断。

### Right Edge Flyout

只用于轻量场景上下文工具，目前为：

- Camera；
- Weather。

Camera / Weather 入口已经并入 Top Shell 第一层，不再单独放一个右上 Quick Controls 浮块。

需要较宽统计、多级 Tab、趋势图或大量管理参数的功能应进入 Management / Workspace，不继续扩宽 Flyout。

### Information Views

属于“观察城市”，不是“管理城市”。

当前：

`默认 / 地价 / 人口 / 商业 / 道路 / 治安 / 水利`

入口位于 Top Shell 第二层的图层图标。Palette 从顶部导航下方展开；选择后收起，主要变化发生在世界地图。

地图数据效果当前仍是原型热力渐变，后续应逐步贴合真实街区、建筑、道路和服务覆盖。

## Workspace Browser 当前基准

统一结构：

`Primary Rail + Context Filter + Content Grid`

### Primary Rail

- 一级分类，图标 + 简短文字；
- `全部建筑` 与其它分类完全同样样式；
- 每组显示 5 个分类；
- 左侧竖向刻度切换分类组；
- 鼠标在 Rail 上滚轮可整组切换；
- 切换可见组不自动选择分类。

### Context Filter

- 顶部纯文字；
- 与 Primary Rail 是正交筛选维度；
- 切换一级分类不能偷偷重置顶部筛选。

建筑当前示例：

`全部 / 庑殿 / 歇山 / 悬山 / 硬山 / 攒尖 / 卷棚 / 其他`

### Content Grid

- 建筑当前固定 `3 列 × 2 行 = 6 项 / 组`；
- Card 使用左侧矩形预览 + 右侧名称 / 一条属性；
- 文字是主识别信息；
- 主内容底部使用 `· · ━ · ·` 无页码分页；
- Content Grid 上滚轮整组翻页；
- 不使用无限长建筑列表。

进入 Building Workspace 后：

- Top Shell 只保留 Persistent Status Row；
- Management Navigation Row 隐藏；
- World Utility Toolbar 保留；
- Main Dock 保留；
- Workspace 占据底部 Dock 上方的浏览空间。

## Settings 当前正式基准

正式规则：`Documentation/设置界面规范.md`

### 布局

- 全屏 Settings Space；
- 顶部分类：`显示 / 图形 / 音频 / 操作 / 游戏`；
- 中间单列 ScrollView；
- 不使用双栏设置 Card；
- 普通 Section 不使用金色左边线；
- Footer 只保留 `恢复当前分类默认值`。

### 控件

Slider：

- 可点轨道、连续拖动；
- 支持方向键 / Shift / Home / End；
- 实时数值；
- 不用滚轮改值。

Select：

- 自定义深墨 Dropdown；
- 支持外部关闭、Esc、上下键、Enter；
- 不使用浏览器原生 Select 外观。

Toggle：

- 真正可点击；
- Off 中性灰；
- On 暖金；
- 有 Hover / Pressed / Focus / Disabled。

Binding：

- `操作 + 主要按键 + 次要按键 + 恢复`；
- 支持监听、Esc 取消、Delete 清除、冲突提示；
- 最终 Unity 使用 Input System Rebinding。

### Settings 保存策略

**普通设置：即时生效 + 即时保存。**

因此普通 Settings 状态下：

```text
Esc / 返回
→ 直接退出
```

不再存在未保存 Draft，也没有常驻 `取消 / 应用`。

**可回滚显示设置：临时应用 + Safe Confirmation。**

当前包括：

- 显示模式；
- 显示器；
- 分辨率；
- 刷新率；
- HDR；
- UI Scale。

流程：

```text
Change
→ Temporary Apply
→ 15 秒 Safe Confirmation
→ Keep / Rollback
```

- `保留设置`：提交；
- `恢复原设置`：恢复；
- Esc：恢复；
- 超时：恢复。

### UI Scale

UI Scale 是 Select，不是 Slider：

`80% / 90% / 100% / 110% / 125% / 150%`

Safe Confirmation Layer 必须独立于正在测试的 UI Scale，最终 Unity 应使用固定 Safe System Overlay。

### Esc 优先级

子层如果已经处理并 `preventDefault` Esc，父级 PauseLayer 不得继续消费。

已专门修正 Pause → Settings 的这一问题。

## BuildingPlacement 当前基准

- 一个主 ToolOverlay；
- Terrain Summary 是轻量状态带；
- Position / Massing / Roof 用局部 Tone 区分；
- Tool Dock：`平 / 填 / 高 │ 位 / 层 / 顶 / 面 │ 完成 / 取消`；
- 网格吸附 / 网格显示 / 撤销 / 重做已迁移到全局 World Utility Toolbar；
- OperationHints 在右下 World Utility Toolbar 上方；
- Tool 状态只保留 Top Shell 的 Persistent Status Row；
- Management Navigation、Main Dock、Information Views 隐藏；
- World Utility Toolbar 保留；
- Camera / Weather 仍是独立 Right Edge Flyout，但入口在 Top Shell。

原 Unity 包仍是功能语义参考，不照搬旧视觉。

## 当前 Visual Review

自动 Review 持续覆盖主要 UI 状态，包括：

- Normal Gameplay 双层 Top Shell；
- 第二层 Management Navigation Row 纯图标、无常驻文字；
- 第二层宽度小于 Persistent Status Row；
- Normal Gameplay 右下 World Utility Toolbar；
- Operation Hints 与 World Utility Toolbar 独立且不重叠；
- Grid Snap 从 Normal Gameplay 进入 Building Placement 后保持；
- Workspace / Building Placement 均保留 World Utility Toolbar；
- Building Placement 不再重复局部 Grid / Undo Utility；
- Finance / Policy Management Space + 顶部一级图标导航；
- Information Views / Land Value View；
- Workspace 滚轮分页 / 筛选保持；
- BuildingPlacement 多模式与空间所有权；
- Camera / Weather；
- Pause；
- Archive；
- Settings 显示 / 图形 / 操作 / 游戏；
- 双键位总览 / Listening；
- Select Open；
- Slider Changed；
- Toggle Changed；
- Disabled；
- Restore；
- Safe Confirmation；
- Safe Rollback；
- Safe Keep。

重要 UI 修改固定执行：

1. Build；
2. Visual Review；
3. 下载 Artifact；
4. AI / 开发者实际打开截图人工审图；
5. 截图发现问题先修正再重新 Review；
6. 不只检查目标页面。

不要把“截图任务成功”当成“已经审图”。

## 关键代码

### 顶层

- `src/App.tsx`
- `src/app/ui-state.ts`
- `src/app/scenarios.ts`
- `src/main.tsx`

### Gameplay

- `src/gameplay/GameplayScreen.tsx`
- `src/gameplay/GameplayHUD.tsx`（当前负责统一 Gameplay Top Shell）
- `src/gameplay/ManagementSpace.tsx`
- `src/gameplay/management-model.ts`
- `src/gameplay/city-management.css`
- `src/gameplay/gameplay-top-shell.css`
- `src/gameplay/CommandBar.tsx`（Main Dock + World Utility Toolbar）
- `src/gameplay/world-utility-toolbar.css`
- `src/gameplay/GameplayOperationHints.tsx`
- `src/gameplay/RightEdgeFlyout.tsx`
- `src/gameplay/PauseLayer.tsx`

### Workspace / Tool

- `src/workspace/BuildingWorkspace.tsx`
- `src/workspace.css`
- `src/tools/building-placement/BuildingPlacementOverlay.tsx`
- `src/tools/building-placement/BuildingPlacementDock.tsx`
- `src/tools/building-placement/building-placement.css`

### Settings

- `src/settings/SettingsPanel.tsx`
- `src/settings/settings-panel.css`
- `src/settings/settings-bindings.css`
- `src/settings/settings-safe-confirmation.css`
- `Documentation/设置界面规范.md`

### Archive

- `src/archive/ArchivePanel.tsx`
- `src/archive/archive-panel.css`

### Review

- `.github/workflows/visual-review.yml`
- `scripts/capture-review-v2.mjs`
- `scripts/capture-management-review.mjs`

## 下一步建议

1. 下一阶段继续重构 Main Dock 自身的比例和美术，但不要再把 World Utility Toolbar 合并回 Main Dock；
2. 继续深化 Management Space 各系统的真实信息架构，不让所有页面只是同一模板换文字；
3. 将 Information Views 从原型渐变推进到更贴合街区 / 建筑 / 道路的可分析数据表现；
4. 继续验证 Management Space 与未来城市详情、选中对象详情之间的边界；
5. 最后做 Visual Token / CSS Cleanup，清理退出运行路径的旧 CityManagementRail / QuickControls / 旧 command-utility 样式兼容层。

## 不要回退

不要重新加入：

- 左侧独立 City Management Rail；
- 右上独立 Quick Controls 浮块；
- Management Navigation Row 常驻文字标签；
- Management Surface 内重复一级管理 Tab；
- 把财政 / 政策 / 军务等复杂系统塞回 Right Edge Flyout；
- Workspace / Tool 状态下常驻 Management Navigation Row；
- 在 Building Placement 内重新复制网格吸附 / 网格显示 / Undo / Redo；
- 把完成 / 取消 / 确认等任务流程按钮塞入 World Utility Toolbar；
- 常驻说明小字；
- 01 / 02 / 03 / 04 装饰编号；
- 无意义右箭头；
- 英文气氛眉题；
- 参数 / 内容层层 Card；
- 粗金边、卷轴、木纹、云纹等仿古装饰；
- `全部建筑` 特殊入口；
- Workspace 数字页码；
- 无限长建筑滚动列表；
- Settings 双栏 Card；
- Settings 常驻 `取消 / 应用`；
- UI Scale Slider；
- 静态不可操作的 Slider / Select / Toggle；
- 危险显示设置永久写入且无法回滚；
- Safe Confirmation 跟随玩家 UI Scale 一起缩放；
- 为了 Web CSS 方便而新增无法合理映射到 Unity 的核心结构。

## 新对话阅读顺序

1. `AGENTS.md`
2. `README.md`
3. 本文件
4. `Documentation/项目概览.md`
5. `Documentation/UI Toolkit落地规范.md`
6. `Documentation/UI设计原则.md`
7. `Documentation/UI空间与状态架构.md`
8. `Documentation/设置界面规范.md`（如果任务涉及 Settings）
9. `Documentation/组件设计规范.md`
10. 当前任务相关代码 / 决策记录 / 开发记录
